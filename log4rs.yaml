refresh_rate: 60 seconds

appenders:
  stdout:
    kind: console
    encoder:
      pattern: "{d(%Y-%m-%d %H:%M:%S %Z)(utc)} |{h({l:5.5})}| {f}:{L} - {m}{n}"

root:
  level: info
  appenders:
    - stdout